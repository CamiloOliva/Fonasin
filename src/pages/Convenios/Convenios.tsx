import {useMemo,useState} from 'react';import {convenios,ConvenioCategory} from '../../data/convenios';const cats:Array<'Todos'|ConvenioCategory>=['Todos','Salud y bienestar','Funerarios','Turismo','Servicios vehiculares'];export default function Convenios(){const[cat,setCat]=useState<typeof cats[number]>('Todos');const items=useMemo(()=>cat==='Todos'?convenios:convenios.filter(c=>c.category===cat),[cat]);return <div className="py-14"><div className="container-page"><span className="text-xs uppercase tracking-[.18em] font-bold text-fonasin-green">Beneficios</span><h1 className="text-4xl sm:text-5xl font-black text-fonasin-deep mt-2">Convenios</h1><p className="mt-4 text-lg text-slate-600 max-w-2xl">Explora los convenios organizados por categoría. Los datos no confirmados están marcados como provisionales.</p><div className="flex flex-wrap gap-2 mt-8">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`px-4 py-2.5 rounded-full font-bold text-sm ${cat===c?'bg-fonasin-green text-white':'bg-fonasin-surface text-fonasin-deep hover:bg-fonasin-green/10'}`}>{c}</button>)}</div><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 mt-9">{items.map(c=><article key={c.id} className="p-6 rounded-2xl border bg-white shadow-sm"><img src={c.logo} alt="Logo provisional" className="w-28 h-16 object-contain rounded-lg"/><div className="mt-4 text-xs uppercase font-bold tracking-wider text-fonasin-green">{c.category}</div><h2 className="mt-1 text-xl font-black text-fonasin-deep">{c.name}</h2><p className="mt-2 text-slate-600 leading-6">{c.description}</p></article>)}</div></div></div>}
