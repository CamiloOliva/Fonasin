---
deployment:
  tasks:
    # Reemplazar con la ruta real del dominio principal.
    - export DEPLOYPATH=/home/CPANEL_USER/public_html
    - npm ci
    - npm run build
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R dist/. $DEPLOYPATH
